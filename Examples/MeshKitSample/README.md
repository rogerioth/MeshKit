# MeshKitSample

Cross-platform SwiftUI sample app for MeshKit.

Open `Examples/MeshKitSample/MeshKitSample.xcodeproj` in Xcode, or run `./scripts/build.sh` to build both the package and the sample for macOS, iOS Simulator, and Mac Catalyst.
